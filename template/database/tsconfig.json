{
    "extends": "@repo/tsconfig/base.json",
    "compilerOptions": {
        "incremental": true,
        "module": "ESNext",
        "moduleResolution": "bundler"
    },
    "include": ["**/*.ts"],
    "exclude": ["dist", "build", "node_modules"]
}
