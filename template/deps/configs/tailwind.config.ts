import type { Config } from 'tailwindcss'

const config: Config = {
    content: ['./src/**/*.{html,ts,tsx}', './index.html'],
    theme: {
        extend: {},
    },
    plugins: [],
}

export default config
